# Build artifacts
.build/
.swiftpm/
build/
*.xcodeproj/
*.xcworkspace/
xcuserdata/

# SPM resolved (regenerated on swift build; commit if you want reproducible dep versions)
# Package.resolved IS committed on purpose: it pins the exact revision of
# every dependency, so a signed/notarized release can be rebuilt bit-for-bit
# later. Ignoring it means `make release` resolves whatever upstream tags
# point at that day — the artifact users install is then unreproducible.

# macOS
.DS_Store

# Local agent / IDE state (machine-specific, never commit)
.claude/
.codex/
.vscode/
.idea/
.serena/

# Distribution artifacts
*.dmg
*.zip
# generated by `make publish` (uploaded to the GitHub Release); must be
# ignored or the leftover file trips publish's clean-tree guard next time
checksums-*.txt

# Local config / secrets
config.toml
config.local.toml
*.env
.env*

# Test artifacts
.test-output/

# graphify knowledge graph (generated, AST-only)
graphify-out/
